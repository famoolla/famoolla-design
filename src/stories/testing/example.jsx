import { Box } from '@mui/material';

const BoxExample = () => { 
    return (
        <Box sx={{ bgcolor: 'primary.main', width: 50, height: 50, borderRadius: '5px' }} />
    );
};

export default BoxExample;